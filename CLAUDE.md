## Personal Knowledge Vault

  Kipp maintains a personal knowledge vault at `~/Vault`.
  The strategy file for this project is `~/Vault/Projects/lyric-engine.md`.

  - **"read the vault"** — read that file for strategic context
  - **"update the vault"** — write decisions/status back to it

  See `~/Vault/CLAUDE.md` for full vault structure and conventions.

  ## Daily Session Logs

  At the end of every session, append a summary to `~/Vault/Daily/YYYY-MM-DD.md`.
  Use header: `## <Project Name> (Claude Code)`
  If the file exists, append - never overwrite.